const EmergencyStrip = () => (
  <div
    className="w-full py-1.5 text-center text-sm font-bold tracking-wide"
    style={{ background: 'hsl(var(--saffron))', color: 'hsl(var(--navy-dark))' }}
    role="banner"
    aria-label="आपत्कालीन क्रमांक"
  >
    आपत्कालीन क्रमांक: 100
  </div>
);

export default EmergencyStrip;
